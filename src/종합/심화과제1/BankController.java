
package 종합.심화과제1;

public class BankController {
    /*
    int deposit = 0;
    Account[] accounts = new Account[100];

    boolean dodo(String bankaccount, int password, int deposit) {
        Account account = new Account();
        account.bankaccount = bankaccount; //은행 게좌
        account.password = password;    // 패스어드
        account.deposit = deposit; // 입금
    }
    boolean dodo2(String bankaccount, int password, int deposit) {
        Account account = new Account();
        for (int i = 0; i < accounts.length; i++) {
            Account accounts = account[i];
        if (accounts.bankaccount == bankaccount && accounts.password == password) {
            account.deposit += deposit;
        }
        }
    }
    boolean dodo3(String bankaccount, int password, int deposit) {
        Account account = new Account();
        account.bankaccount = bankaccount; //은행 게좌
        account.password = password;    // 패스어드
        account.deposit = deposit;
    }
    boolean dodo4(String bankaccount, int password, int deposit) {
        Account account = new Account();
        account.bankaccount = bankaccount; //은행 게좌
        account.password = password;    // 패스어드
        account.deposit = deposit;
    }

    Account[] doget() { return accounts;} //모든 계좌를 반환 메소드

     */
}